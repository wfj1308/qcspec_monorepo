{
  "name": "qcspec",
  "version": "0.1.0",
  "private": true,
  "description": "QCSpec · 工程质检平台 · GitPeg v:// 生态",
  "workspaces": [
    "apps/*",
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "dev":     "turbo run dev",
    "build":   "turbo run build",
    "test":    "turbo run test",
    "test:component-utxo:gate": "python tools/acceptance/component_utxo_gate.py --skip-demo",
    "acceptance:component-utxo:demo": "python tools/acceptance/component_utxo_beam_l3_demo.py --skip-docpeg --payload-file tools/acceptance/config/component_utxo_beam_l3.sample.json",
    "lint":    "turbo run lint",
    "db:push": "supabase db push",
    "db:reset":"supabase db reset"
  },
  "devDependencies": {
    "turbo":      "^1.13.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
